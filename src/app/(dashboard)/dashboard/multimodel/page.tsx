import MultiModelStudio from "./MultiModelStudio";

export const metadata = {
  title: "Multi-Model Studio | YahiaRoute",
  description: "Orchestrate, route, and run multiple AI models together on the same task with YahiaRoute.",
};

export default function MultiModelPage() {
  return <MultiModelStudio />;
}
